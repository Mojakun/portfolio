overwrite: true
schema: "https://basick-lesson-hasura.hasura.app/v1/graphql"
documents: "src/queries/**/*.ts"
generates:
  src/types/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"

